{% extends 'base2.html.twig' %}
{% block title %}List{% endblock %}
{% block name %}Resultat de la recherche{% endblock %}
{% block stylesheets %}
           <link href="{{ asset('css/main.css') }}" rel="stylesheet" />
        {% endblock %}
{% block body %}
{% if t %}   
   <table class="table table-striped">
  <thead>

    <tr>
      <th scope="col">Id</th>
      <th scope="col">Name</th>
      <th scope="col">Matricule</th>
      <th scope="col">e-mail</th>
      <th scope="col">Pannes</th>

    </tr>
  </thead>
  <tbody>
   
    <tr>
      <th scope="row">{{ t.id }}</th>
    
      <td>{{ t.nom }}</td>
      <td>{{ t.matricule }}</td>
      <td>{{ t.mail }}</td>
      <td>{% for i in t.pannes %}
              <td>{{ i.id|trans ~ ""}}</td>
         {% endfor %}
         </td>


    
  <td>
    <a class="btn btn-primary" href="{{ path('technicien_edit', { 'id' : t.id|default('1') }) }}">
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      Edit
  </a>
      {{ include('technicien/delete.html.twig') }}


         </td>
         </tr>
 </tbody>
</table>
         <center><a  class="btn btn-success"href="{{ path('techpannes', { 'id' : t.id|default('1') })}}">{{ ("pannes") }}</a></center>


  {% else %}
     Id incorrecte, choisir un autre id 
  {% endif %}   
 
{% endblock %}
