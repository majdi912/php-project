{% extends 'base2.html.twig' %}
{% block title %}List{% endblock %}
{% block name %}Resultat de la recherche{% endblock %}
{% block stylesheets %}
           <link href="{{ asset('css/main.css') }}" rel="stylesheet" />
        {% endblock %}
{% block body %}
{% if h %}   
   <table class="table table-striped">
  <thead>

    <tr>
      <th scope="col">Id</th>
      <th scope="col">ip</th>
      <th scope="col">mac</th>
      <th scope="col">name</th>
      <th scope="col">location</th>
      <th scope="col">status</th>
     

    </tr>
  </thead>
  <tbody>
   
    <tr>
      <th scope="row">{{ h.id }}</th>
    
      <td>{{ h.ip }}</td>
      <td>{{ h.mac }}</td>
      <td>{{ h.nom }}</td>
      <td>{{ h.location }}</td>
      <td>{{ h.status }}</td>
      
     {# <td>{% for i in h.pannes %}
              <td>{{ i.type|trans ~ ""}}</td>
         {% endfor %}
         </td>#}
         {% if is_granted('ROLE_ADMIN') %}
    

         <td>
    <a class="btn btn-primary" href="{{ path('host_edit', { 'id' : h.id|default('1') }) }}">
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      Edit
  </a>
        {{ include('host/delete.html.twig') }}

  <a class="btn btn-success" href="{{ path('ping_host' ,{ 'id' : h.id|default('1') }) }}">
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      ping</a>
      <a class="btn btn-danger" href="{{ path('reboot_host' ,{ 'id' : h.id|default('1') }) }}">
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      reboot</a>
      
         </td>
{% endif %}
    </tr>
  
 </tbody>
 
</table>
    

  {% else %}
     Id incorrecte, choisir un autre id 
  {% endif %}   
 <center><a  class="btn btn-success"href="{{ path('hostpannes', { 'id' : h.id|default('1') })}}">{{ ("pannes-history") }}</a></center>
{% endblock %}
